## Training Directory

**This directory contains the training data (in `.json` format).**

---

Table of files/directories

|Names|Description|Location|
|-----|-----------|--------|
|Old Training|the old training data for _nenode_|[`OLD-training.json`](OLD-training.json)|
|New Training data|the new data for _nenode_ training|[`../public/data.json`](../public/data.json)|
